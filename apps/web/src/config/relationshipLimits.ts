/** Max items for Sent / Skipped list views (newest-first; server should align). */
export const RELATIONSHIP_LIST_LIMIT = 30;

/** Initial batch for Matches grid; "Load more" adds this many. */
export const MATCHES_PAGE_SIZE = 30;
