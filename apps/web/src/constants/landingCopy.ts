/** Shared landing conversion copy — keep CTAs consistent. */
export const LANDING_PRIMARY_CTA = 'Find My Training Partner';
/** Hero line under primary CTA for guests (opens preview modal). */
export const LANDING_HERO_SUB_GUEST = 'Free preview • No signup required';
/** Secondary line on other landing CTAs and logged-in hero stack. */
export const LANDING_CTA_SUB = 'Takes 30 seconds • No commitment';
export const LANDING_SCARCITY = 'Only 5 new matches shown per day on free plan';
