/** Shared landing conversion copy — keep CTAs consistent. */
export const LANDING_PRIMARY_CTA = 'Start Matching Now';
/** Hero line under primary CTA for guests (opens preview modal). */
export const LANDING_HERO_SUB_GUEST = 'No signup required • Free preview';
/** Secondary line on other landing CTAs and logged-in hero stack. */
export const LANDING_CTA_SUB = 'No signup required • Preview first';
/** Subtle urgency for free tier (marketing only). */
export const LANDING_SCARCITY = 'Free preview shows a limited set of new profiles each day.';
